<p id="notice"><%= notice %></p>

<h1>Listing Pujas</h1>

<table>
  <thead>
    <tr>
      <th>Product</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pujas.each do |puja| %>
      <tr>
        <td><%= puja.product_id %></td>
        <td><%= puja.user_id %></td>
        <td><%= link_to 'Show', puja %></td>
        <td><%= link_to 'Edit', edit_puja_path(puja) %></td>
        <td><%= link_to 'Destroy', puja, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Puja', new_puja_path %>
